# ОС
Thumbs.db
.DS_Store

# Zed
.zed/
.agents/  # проектные skill-дубли — не публиковать (см. rules-Zed.md §4)

# Временные файлы
*.tmp
tmp*
nul  # Windows: артефакт редиректа "cmd /c ... > nul" (нулевой файл в корне)
