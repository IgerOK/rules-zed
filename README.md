# Settings Zed

Проект конфигурации AI-агента Zed IDE. Единый источник истины для
переноса настроек на чистую машину: глобальные правила (с блоками
«Персона и стандарты качества», «Project Bootstrapping» и
«Loop Prevention & Token Economy»), skills
`git-workflow` и `project-doctor`, настройка провайдера/модели и иллюстрация.

## Состав

| Файл | Назначение |
|---|---|
| [`rules-Zed.md`](rules-Zed.md) | Объединённая конфигурация Zed-агента: §1 глобальный `AGENTS.md` (включая блоки `Персона и стандарты качества`, `Project Bootstrapping`, `Loop Prevention & Token Economy`), §2 skill `git-workflow`, §3 провайдер и модель (рекомендуется Agnes, допустим любой LLM-агрегатор; reasoning effort **HIGH** для кода, `MEDIUM/LOW` — для документации, контекст **512K** (524288); также 3 альтернативных провайдера для бесплатной/дешёвой отработки: B.AI, NaraRouter, dahl.global), §4 нюансы (skills, обновления Zed, проектный `AGENTS.md`), §5 чек-лист проверки, §6 skill `project-doctor` (4-шаговая диагностика) |
| [`agnes1high.png`](agnes1high.png) | Скриншот настройки провайдера в Zed (Settings → Models → API Providers): OpenAI-совместимый эндпоинт Agnes, модель **Agnes 3.0 Flash** с reasoning effort **High** (ключ маскирован, хранится в системном keychain, не в `settings.json`) |
| [`.gitignore`](.gitignore) | Игнорируемые файлы для публикации в Git (ОС-хлам, служебные каталоги Zed, временные файлы) |
| [`LICENSE`](LICENSE) | Лицензия проекта — MIT (английский оригинал + неофициальный перевод на русский) |

## Как использовать (новая машина)

1. Установить Zed: https://zed.dev/?ref=taaft (Windows — инсталлятор
   `Zed-x.x.x-windows-installer.exe` или портативный `.zip`; macOS — `.dmg`;
   Linux — `tar.xz`). Обновление — ставить **поверх** (не удалять `AppData\Roaming\Zed`
   и `AppData\Local\Zed` — там настройки, глобальный `AGENTS.md`, история чатов).
   Текущая версия: `Ctrl+Shift+P` → `Zed: About`.
2. Установить Git (если отсутствует): https://git-scm.com (Windows — инсталлятор;
   Linux — системный пакетный менеджер `apt`/`dnf`/`pacman`; macOS — `brew install git`).
3. Скопировать `rules-Zed.md` на машину и выполнить его инструкции §1–§6
   (вручную или передать их агенту Zed целиком).
4. Настроить провайдера — это ручной шаг в Settings → Models (§3 документа:
   базовый URL/модель, Reasoning effort **HIGH** и Context limit **512K**);
   API-ключ вставляется только туда и не попадает в файлы.
5. Открыть **новый** агент-чат: список skills и правил фиксируется при создании
   треда и в уже открытых диалогах не обновляется (перезапуск Zed не помогает —
   старые чаты восстанавливаются со «замороженным» снимком).

## Проектные правила

Глобальный `AGENTS.md` покрывает общие правила. Для нового кодинг-проекта в его
корень целесообразно добавить файл `AGENTS.md` **с дополнительным** описанием —
записывать туда только проектно-специфичное (то, что отличается от глобального, без дублирования):
- стек и версии (язык, фреймворки, версии тулов);
- точные команды сборки/запуска/тестов/линтинга (с флагами);
- конвенции проекта (именование, структура каталогов, запретные зоны);
- особенности окружения (имена переменных окружения **без значений**, порты, БД, CI);
- требования к формату ответов/отчётов.

Не дублировать глобальные правила (commit message, git check, лимиты моделей) —
источник правды один (глобальный `AGENTS.md`); держать файл актуальным и не
записывать в него секреты. Быстрый старт: попросить агента написать начальный
проектный `AGENTS.md` по правилам из глобального, затем проверить и дополнить.

## Конфиденциальность

Файлы проекта не содержат секретов: API-ключи и токенные значения передаются
только через настройки Zed (system keychain), а в документах стоят плейсхолдеры
(`<USER>`, `User Name`, `email@example.com`, «ваш ключ LLM»).

## Лицензия

MIT — см. [`LICENSE`](LICENSE).
